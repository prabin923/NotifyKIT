[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "notification-platform-sdk"
version = "0.1.0"
description = "Python SDK for the Universal Notification Platform"
requires-python = ">=3.9"
