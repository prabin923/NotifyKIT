import { DashboardShell } from '../../components/dashboard-shell'; import { LoginGate } from '../../components/login-gate'; import { ResourcePage } from '../../components/resource-page'; export default function Page() { return <LoginGate><DashboardShell><ResourcePage title="Providers" endpoint="/v1/dashboard/providers" description="Configured provider modes. Credentials are never shown." /></DashboardShell></LoginGate>; }
